# nixpacks.toml
# Adiciona ferramentas de compilação nativas para o better-sqlite3 (node-gyp).
# aptPkgs é adicionado ao ambiente SEM substituir o Node.js detectado automaticamente.

[phases.setup]
aptPkgs = ["python3", "build-essential"]
