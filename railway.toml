[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm rebuild better-sqlite3 --update-binary"

[deploy]
startCommand = "npm run server"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
